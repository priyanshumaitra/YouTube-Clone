const scrollbar = document.querySelector(".sidebar")
scrollbar.addEventListener("mouseenter",()=>{
    scrollbar.classList.toggle("scroll");

})
scrollbar.addEventListener("mouseleave",()=>{
    scrollbar.classList.toggle("scroll");

})